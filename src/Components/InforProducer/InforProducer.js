import classNames from 'classnames/bind';
import styles from './InforProducer.module.scss';
const cx = classNames.bind(styles);

function InforProducer() {
    return <div className={cx('wrapper')}>InforProducer</div>;
}

export default InforProducer;
